write_files:
  - path: "/usr/local/etc/init.sh"
    permissions: "755"
    content: |
      #!/bin/bash
      sed -i 's/^#Port 22/Port 2222/' /etc/ssh/sshd_config
      systemctl restart sshd
    defer: true
runcmd:
  - [su, external, -c, "/usr/local/etc/init.sh"]